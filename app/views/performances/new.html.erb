<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New performance</h1>

  <%= render "form", performance: @performance %>

  <%= render(ButtonComponent.new(url: performances_path)) { 'Back to performances' } %>
</div>
